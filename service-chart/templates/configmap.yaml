apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "service-chart.fullname" . }}
  labels:
    {{- include "service-chart.labels" . | nindent 4 }}
{{- with .Values.env }}
data:
    {{- toYaml . | nindent 2 }}
{{- end }}
